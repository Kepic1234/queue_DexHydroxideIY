local Active = getgenv().ActiveModules or {}

local function exec(name, script)
    if ScriptConfig[name] == true and not Active[name] then
        Active[name] = true
        script
    end
end

exec("IY", loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))())


exec("Hydroxide", 
        local owner = "Upbolt"
        local branch = "revision"
        
        local function webImport(file)
                return loadstring(game:HttpGetAsync(("https://raw.githubusercontent.com/%s/Hydroxide/%s/%s.lua"):format(owner, branch, file)), file .. '.lua')()
        end
        
        webImport("init")
        webImport("ui/main")
    )


exec("Cobalt", loadstring(game:HttpGet("https://github.com/notpoiu/cobalt/releases/latest/download/Cobalt.luau"))())


exec("DexV3", loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))())


exec("latestDex", loadstring(game:HttpGet("https://github.com/AZYsGithub/DexPlusPlus/releases/latest/download/out.lua"))())



getgenv().ActiveModules = Active
getgenv().ScriptConfig = ScriptConfig
